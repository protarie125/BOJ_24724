#include <iostream>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	int t;
	cin >> t;
	for (auto i = 1; i <= t; ++i) {
		cout << "Material Management " << i << '\n';
		cout << "Classification ---- End!\n";

		int n;
		cin >> n;

		int a, b;
		cin >> a >> b;
		while (0 < (n--)) {
			int u, v;
			cin >> u >> v;
		}
	}

	return 0;
}